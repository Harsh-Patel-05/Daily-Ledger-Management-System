SECRET_KEY=django-insecure-change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_ENGINE=django.db.backends.postgresql
DB_NAME=daily_ledger
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Or use a single URL (overrides DB_* above), e.g. Render:
# DATABASE_URL=postgres://user:pass@host:5432/daily_ledger
